export default class Person {
    constructor(fullname, email, experience, education) {
        this.fullname = fullname
        this.email = email
        this.experience = experience
        this.education = education
    }
}